# MongoDB Atlas -> Connect -> Drivers -> copy SRV connection string (replace password)
MONGODB_URI=mongodb+srv://prabhu:panda%402005@emotion.k5nfdfi.mongodb.net/?retryWrites=true&w=majority&appName=emotion


MONGODB_DB_NAME=emotion_system

# Base URL the browser uses to load /files/{id} (must match how you open the API)
API_PUBLIC_URL=http://localhost:8000

# Admin auth (set to enable /admin login)
ADMIN_PASSWORD=prabhu@2005
ADMIN_JWT_SECRET=secretkey123
